{% extends 'user/index.html.twig' %}
{% block tabpanel %}
    <div class="tab-pane fade show active" id="address" role="tabpanel"
         aria-labelledby="change-password-list">
        <div class="d-flex justify-content-between align-items-center border-bottom">
            <div class="fs-6 fw-bold">Address</div>
            <div class="my-2">
                <button class="btn btn-dark rounded-pill" type="button" data-bs-toggle="modal" data-bs-target="#addressModal">
                    <i class="fas fa-plus-circle"></i> <span>New Address</span>
                </button>
            </div>
        </div>
        <div class="row row-cols-1 g-4 mt-0">
            {% include 'user/_addresses.html.twig' %}
        </div>
    </div>
{% endblock %}